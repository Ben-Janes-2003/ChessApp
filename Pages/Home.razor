@page "/"
@using ChessApp.Models

<div class="chess-board">
    @for (int row = 0; row < 8; row++)
    {
        @for (int col = 0; col < 8; col++)
        {
            bool isDark = (row + col) % 2 != 0;
            <div class="square @(isDark ? "dark" : "light")">
                @{
                    Piece? piece = Board.Spaces[row, col];
                    char? symbol = piece?.Symbol;
                    if (symbol is not null && piece is not null)
                    {
                        <div class="piece @(piece == Board.SelectedPiece ? "selected" : "")" @onclick="() => SelectPiece(piece)">
                            @symbol
                        </div>
                    }
                }
            </div>
        }
    }
</div>